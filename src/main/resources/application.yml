server:
  port: 8080

# 连接数据库
spring:
  datasource:
    driver-class-name: com.mysql.jdbc.Driver
    url: jdbc:mysql://localhost:3306/demosprboot?useUnicode=true&characterEncoding=UTF-8&useSSL=false
    username: root
    password: root

# 整合mybatis
mybatis:
  config-location: mybatis-config.xml
  mapper-locations: /mapper/**.xml
  type-aliases-package: com.demo.tempsprboot.pojos